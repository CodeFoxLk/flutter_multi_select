import 'package:flutter/material.dart';

class MultiSelectCheckList extends StatelessWidget {
  const MultiSelectCheckList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Multi Select Check List'),),
    );
  }
}